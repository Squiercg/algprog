#include <stdio.h>

#define MAX 100

void multmat(int A[MAX][MAX] , int B[MAX][MAX], int C[MAX][MAX], int n) {
  int i, j, k;

  for(i=0;i<n;i++){
    for(j=0;j<n;j++){
      C[i][j]=0;
      for(k=0;k<n;k++){
	C[i][j]=C[i][j]+A[i][k]*B[k][j];
      }
    }
  }

}


int main(void)
{

  return 0;
}
