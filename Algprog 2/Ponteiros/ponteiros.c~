#include <stdio.h>
#include <stdlib.h>

int main(void)
{
  int i, n;
  float vetor[100];
  scanf("%d",&n);

  for(i=0;i<n;i++){
    vetor[i]=rand()%50;
  }

  for(i=0;i<n;i++){
    printf("%f",vetor[i]);
  }

  return 0;
}
