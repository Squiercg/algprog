#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main(int argc, char const *argv[]) {

  pid_t pid = fork();


  if(pid == 0) {
    printf("Sou filho %d\n",pid);
    while(1){

    }

  } else {
    printf("Sou pai: %d\n",pid)
    while(1){

    }


  }


  return 0;
}
